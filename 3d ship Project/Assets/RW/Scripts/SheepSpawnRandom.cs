using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SheepSpawnRandom : MonoBehaviour
{[SerializeField] private GameObject sheepRandom;
    [SerializeField] private List<SheepProperty> 
    //private void SetSheepProperty()
    //{
    //    Instantiate(SheepProperty(random.count.length));
    //}

    // Update is called once per frame
    void Update()
    {
        
    }
}
